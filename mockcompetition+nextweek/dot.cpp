#include <iostream>
#include <vector>
using namespace std;

int smallest(vector<int> x, vector<int> y) {
	return 0;
}

int main() {

	int x;
	cin >> x;
	while (x--) {
		int size;
		cin >> size;
		for (int i = 0; i < size; i++) {
			cin >> 
		}
	}
}